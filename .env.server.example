DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=frankbria_strapi
DATABASE_USERNAME=strapi_user
DATABASE_PASSWORD=change_me

# Strapi encryption keys (4 comma-separated keys required for session management)
# Generate with: node -p "require('crypto').randomBytes(16).toString('hex')"
APP_KEYS=key1,key2,key3,key4
